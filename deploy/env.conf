# systemd EnvironmentFile 로 로드 (키=값만)
# JAVA_TOOL_OPTIONS: JVM 이 자동으로 읽음 (ExecStart 에 옵션을 안 적어도 됨)
JAVA_TOOL_OPTIONS=-Xms64m -Xmx128m -XX:+UseG1GC
SPRING_PROFILES_ACTIVE=default
SPRING_CONFIG_ADDITIONAL_LOCATION=optional:file:/opt/demo-ci/current/application.yml
SERVER_PORT=8080
